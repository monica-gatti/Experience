My Unit Test was not able to get the file imported with "FROM" ("ModuleNotFoundError: No module named X")

1.CTRL + SHIFT + P
2. Choose User Setting
3.Add
{
    "terminal.integrated.env.windows": { "PYTHONPATH": "${workspaceFolder}" }
}
